# Build artifacts
dist/
build/
*.egg-info/
__pycache__/
*.pyc

# Local test scene (1.8 GB, not part of the package)
*.nc

# Local secrets / one-off push helpers (never commit)
_gh_token.txt
_api_push.py

# Local run outputs
output/
